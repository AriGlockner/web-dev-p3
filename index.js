import React from 'react';
import ReactDOM from 'react-dom';
import DisplayPlayerData from './Display.jsx';

ReactDOM.render(
    <React.StrictMode>
        <DisplayPlayerData />
    </React.StrictMode>,
    document.getElementById('root')
);
